# Edinburgh Festivals Listings API credentials.
# 1. Log in to https://api.edinburghfestivalcity.com and create a project key.
# 2. Copy the API key value into EDINBURGH_FESTIVALS_API_KEY.
# 3. Copy the Secret signing key value into EDINBURGH_FESTIVALS_SECRET_KEY.
# 4. Save a real copy in .env (do not commit .env).
EDINBURGH_FESTIVALS_API_KEY=your_api_key_here
EDINBURGH_FESTIVALS_SECRET_KEY=your_secret_signing_key_here

# Optional: default festival filter. Leave blank to fetch all festivals.
EDINBURGH_FESTIVALS_DEFAULT_FESTIVAL=

# OpenWeatherMap API credentials.
# 1. Create or log in to your account at https://home.openweathermap.org/api_keys.
# 2. Generate an API key and paste it into OPENWEATHERMAP_API_KEY.
# 3. Keep the real value inside .env (never commit it).
OPENWEATHERMAP_API_KEY=your_openweathermap_api_key_here
