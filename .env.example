# Edinburgh Festivals Listings API credentials.
# 1. Log in to https://api.edinburghfestivalcity.com and create a project key.
# 2. Copy the API key value into EDINBURGH_FESTIVALS_API_KEY.
# 3. Copy the Secret signing key value into EDINBURGH_FESTIVALS_SECRET_KEY.
# 4. Save a real copy in .env (do not commit .env).
EDINBURGH_FESTIVALS_API_KEY=your_api_key_here
EDINBURGH_FESTIVALS_SECRET_KEY=your_secret_signing_key_here

# Optional: default festival filter. Leave blank to fetch all festivals.
EDINBURGH_FESTIVALS_DEFAULT_FESTIVAL=

# OpenWeatherMap API credentials.
# 1. Create or log in to your account at https://home.openweathermap.org/api_keys.
# 2. Generate an API key and paste it into OPENWEATHERMAP_API_KEY.
# 3. Keep the real value inside .env (never commit it).
OPENWEATHERMAP_API_KEY=your_openweathermap_api_key_here

# OpenAI API key for LLM-backed activity suggestions via OpenRouter.
# 1. Create an OpenAI API key at https://platform.openai.com/api-keys (or reuse an existing one).
# 2. Store the key in OPENAI_API_KEY inside your local .env file.
# 3. Never commit real secret values—only placeholders belong in this template.
OPENAI_API_KEY=your_openai_api_key_here

# Google Maps Platform API key used for geocoding.
# 1. Visit https://console.cloud.google.com/apis/credentials and create an API key (or reuse an existing one).
# 2. Enable the Geocoding API for the project and restrict the key to that API if possible.
# 3. Paste the key into GOOGLE_MAPS_API_KEY in your local .env file (never commit the real value).
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# X (formerly Twitter) API bearer token for tweet scraping.
# 1. Apply for a project at https://developer.x.com/ and create an app with read permissions.
# 2. Generate a bearer token (labeled "App access token") and keep it somewhere safe.
# 3. Add the token to X_API_KEY inside your .env so tests and scrapers can authenticate.
X_API_KEY=your_x_api_key_here
